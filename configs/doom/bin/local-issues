#!/bin/sh
set -eu

launcher_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
exec emacs --batch -Q \
  -L "$launcher_dir/../lisp" \
  -l local-issues-core \
  --eval '(local-issues-cli-main)' \
  -- "$@"
